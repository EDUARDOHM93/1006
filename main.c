#include <stdio.h>

int main(void) {

		double nota1;
		double nota2;
		double nota3;
	scanf("%lf %lf %lf",&nota1 , &nota2, &nota3);
		double MEDIA = nota1 * 2 + nota2 * 3 + nota3 *5;
	printf("MEDIA = %.1f\n",MEDIA /10);

	return 0;

}
	
